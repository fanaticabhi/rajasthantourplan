<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhi.rajasthantourplan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tourdates" />
        <activity android:name=".Events" />
        <activity android:name=".hotels" />
        <activity android:name=".transport_new" />
        <activity android:name=".webview" />
        <activity android:name=".touristplaces" />
        <activity android:name=".completejsondata" />
        <activity android:name=".listview" />
        <activity android:name=".apionlinedata" />
        <activity android:name=".secondcompletejsondata" />
        <activity android:name=".tourschedule"></activity>
    </application>

</manifest>